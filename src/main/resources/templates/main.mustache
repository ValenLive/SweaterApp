<html>
<head>
    <title>
        Example of Paragraph tag
    </title>
</head>
<body>
<!--Log out form-->
<div>
    <form method="post" action="/logout">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <input type="submit" value="Log out">
    </form>
</div>
<!--Adding item to DB from-->
<div>
    <form method="post" action="/main">
        <input type="text" name="text" placeholder="Enter text here">
        <input type="text" name="tag" placeholder="Enter tag here">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Add</button>
    </form>
</div>
<!--Find item by tag form-->
<div>
    <form method="post" action="filter">
        <input type="text" name="text" placeholder="Enter tag here">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Find</button>
    </form>
</div>
<!--Removing item from DB-->
<div>
    <form method="post" action="remove">
        <input type="text" name="id" placeholder="Enter id here">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Remove</button>
    </form>
</div>
<!--Sorting by id-->
<div>
    <form method="post" action="sortByIdDesc">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Sort by ID from highest to lowest</button>
    </form>
    <form method="post" action="sortByIdAsc">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Sort by ID from lowest to highest</button>
    </form>
</div>
<i>
    {{#message}}
        {{message}}
    {{/message}}
</i>
<div>List of messages:</div>
<!--Відтворення даних які були внесені в базу даних-->
{{#messages}}
    <div>
        <b>{{id}}</b>
        <span>{{text}}</span>
        <i>{{tag}}</i>
        <strong>{{authorName}}</strong>
    </div>
{{/messages}}
</body>
</html>